includes += include_directories('.')
includes += include_directories('lice')
includes += include_directories('libpng')
includes += include_directories('zlib')
sources += files(
# apparently we don't build this
)
