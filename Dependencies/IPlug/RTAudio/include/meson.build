includes += include_directories('.')

system = host_machine.system()

if system == 'windows'
  sources += files(
    'asio.cpp',
    'asiodrivers.cpp',
    'asiolist.cpp',
    'iasiothiscallresolver.cpp',

  )
endif
